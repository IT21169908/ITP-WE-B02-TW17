import React from 'react';
import styles from './Layout.module.scss';

function Footer(props: any) {
    return (
        <div className={styles.test}>This is Footer</div>
    );
}

export default Footer;
