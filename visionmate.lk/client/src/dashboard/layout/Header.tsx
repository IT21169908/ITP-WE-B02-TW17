import React from 'react';
import styles from './Layout.module.scss';

function Header(props: any) {
    return (
        <div className={styles.test}>This is Header</div>
    );
}

export default Header;
