import React from 'react';
import { Routes } from 'react-router-dom';

function GuestRoutes(props: any) {
    return (
        <>
            <Routes>
                {/*<Route path="/*" element={<Auth />} />{' '}*/}
            </Routes>
        </>
    );
}

export default GuestRoutes;
